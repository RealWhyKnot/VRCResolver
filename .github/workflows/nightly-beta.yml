name: Nightly beta

on:
  schedule:
    - cron: '10 6 * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: nightly-beta-release
  cancel-in-progress: false

jobs:
  tag:
    uses: RealWhyKnot/workflows/.github/workflows/nightly-beta-scripted.yml@v1
    with:
      runs-on: windows-latest
      validate-scripts: |
        ./.github/scripts/Test-WorkflowSyntax.ps1
        ./.github/scripts/Test-NightlyBetaPlan.ps1
        ./.github/scripts/Test-UpdateChangelog.ps1
        ./.github/scripts/Test-GenerateReleaseNotes.ps1
      no-changes-message: No proxy changes since the latest release tag.
